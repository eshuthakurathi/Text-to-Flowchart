# 🚀 Text-to-Flowchart Generator

An AI-powered system that converts natural language into structured flowchart diagrams.

This project uses:

- 🧠 Ollama (Mistral LLM) to generate Mermaid flowchart code from text
- 📊 Mermaid for flowchart syntax
- 🐳 Kroki (Docker-based) for rendering flowchart images
- 🐍 Python (Flask) backend API

---

## 📌 Overview

Text-to-Flowchart is a self-hosted AI application that takes a user prompt and automatically:

1. Generates Mermaid flowchart code using a local LLM (Mistral via Ollama)
2. Sends the Mermaid code to Kroki
3. Renders and stores the generated flowchart image

This enables automated diagram generation from plain English descriptions.

---

## 🏗️ Architecture

```
User Input
↓
Mistral (Ollama)
↓
Mermaid Code
↓
Kroki (Docker)
↓
Flowchart Image (PNG/SVG)
```

---

## 📂 Project Structure

```
app/
├── mistral.py # Handles LLM interaction via Ollama
├── flowchart-generator.py # Sends Mermaid code to Kroki

Docker/
└── docker-compose.yml # Runs Kroki service

static/images/ # Stores generated flowchart images

main.py # Flask entry point
requirements.txt # Python dependencies
```

---

## ⚙️ Setup Instructions

### 1️⃣ Clone the repository

```bash
git clone https://github.com/eshuthakurathi/Text-to-Flowchart.git
cd Text-to-Flowchart
```

2️⃣ Create Virtual Environment

```bash
python -m venv venv
source venv/bin/activate   # Mac/Linux
venv\Scripts\activate      # Windows
```

3️⃣ Install Dependencies

```bash
pip install -r requirements.txt
```

4️⃣ Start Kroki using Docker

```bash
docker-compose up -d
```

5️⃣ Run the Application

```bash
python main.py
```

🧪 Example Usage

Input: Create a flowchart for user login authentication process.

Output:

- Mermaid flowchart generated by Mistral
- Rendered PNG/SVG diagram via Kroki

🛠️ Technologies Used

- Python
- Flask
- Ollama
- Mistral LLM
- Mermaid
- Kroki
- Docker

🎯 Use Cases

- Automated documentation
- Software architecture visualization
- Educational tools
- AI-assisted diagram generation

🔐 Self-Hosted & Local AI

This project runs entirely on local infrastructure:

- No external API calls
- No OpenAI dependency
- Fully offline-compatible

📈 Future Improvements

- Web UI for interactive flowchart editing
- Support for sequence and class diagrams
- Export to PDF
- Prompt optimization

👨‍💻 Author

Eshu Thakurathi
B.Tech CSE (AI/ML) Student


⭐ If you found this useful
Consider giving it a star!
